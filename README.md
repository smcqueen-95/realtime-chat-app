# realtime-chatapp

## Build Setup

```bash
# install dependencies
$ npm install

# start with expo to localhost:1992
$ expo start

# start with android
$ expo start --android

# strat with iphone
$ expo start --ios

# start with web
$ expo start --web
```